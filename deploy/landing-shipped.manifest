# What a storefront is expected to serve. Anything else stops the deploy before
# a byte goes up — see deploy/check-shipped-files.py for why "stop" rather than
# "delete".
#
# Written because five of our own check scripts answered 200 on both production
# storefronts for months: the landing directory ships whole, and there was no
# list to hold it against. The prune added the same day removes what the build
# drops; it never questions what the build keeps. This does.

# the pages themselves — English at the root, one folder per language
index.html
legal.html
rules.html
report.html
404.html            # sosed ships one, neighbro does not — see D6 in open-work
*/index.html

# generated at deploy time
config.js
robots.txt
sitemap.xml
sw.js

# typography. The rule is deliberately broad and therefore will NOT catch an
# extra font file — three mockup faces would pass through it. What catches those
# is the zone prune and the fact that fonts/ is only ever filled by
# scripts/fetch-fonts.sh.
fonts.css
fonts-georgian.css
fonts-armenian.css
fonts/*.woff2

# pictures: the shared assets and one social sheet per language
img/*.jpg
icons/*
og-image.jpg
og-image-*.jpg
manifest.json

# legal texts, fetched by the pages rather than inlined
legal/*.md

# the revisions those texts are at: date plus digest per document, generated at
# deploy time from deploy/legal-revisions.json. The node fetches it to decide
# whether what a person accepted is still what the document says (29.08.2026).
legal-manifest.json

# IndexNow proves ownership by serving its key at the root. Named in full rather
# than as *.txt: the guard matches with fnmatch, whose * crosses '/', so *.txt
# allowed any .txt at any depth — a rule that reads like «the key» and means «any
# text file anywhere». The key is a constant of this storefront and already
# written out in deploy-landing.sh (INDEXNOW_KEY); when it is rotated, both
# places change, and that is a decision rather than an accident.
8f4c1a7e93d6425bb0e2f5c8a1d73096.txt
