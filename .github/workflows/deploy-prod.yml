name: Deploy prod

# Manual promotion: pick a release tag (already validated on UAT) → deploy prod.
on:
  workflow_dispatch:
    inputs:
      ref:
        description: "Release tag to deploy (e.g. v2026.07.06-abc1234)"
        required: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref }}
      # The runner ships Node, but the page generator should not depend on whichever
      # version that happens to be. RUN_NODE_NATIVE tells run-node.sh to use it directly
      # instead of starting a container.
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Deploy landing
        env:
          BUNNY_STORAGE_ZONE: ${{ secrets.BUNNY_STORAGE_ZONE }}
          BUNNY_STORAGE_API_KEY: ${{ secrets.BUNNY_STORAGE_API_KEY }}
          BUNNY_PULL_ZONE_ID: ${{ secrets.BUNNY_PULL_ZONE_ID }}
          BUNNY_API_KEY: ${{ secrets.BUNNY_API_KEY }}
          RELAY_API_URL: ${{ secrets.RELAY_API_URL }}
          ANALYTICS_ID: ${{ secrets.ANALYTICS_ID }}
          SEARCH_CONSOLE_TOKEN: ${{ secrets.SEARCH_CONSOLE_TOKEN }}
          LANDING_ENV: prod
          RUN_NODE_NATIVE: '1'
        run: bash deploy/deploy-landing.sh
